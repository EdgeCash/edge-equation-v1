"""
Isotonic calibration helper — stub. Replace verbatim during cutover:

    curl -fsSL https://raw.githubusercontent.com/EdgeCash/edge-equation-scrapers/main/exporters/mlb/isotonic.py \\
        -o src/edge_equation/exporters/mlb/isotonic.py

v1 already has its own isotonic at src/edge_equation/math/isotonic.py;
we may end up unifying them rather than porting two implementations.
Decision deferred until the scrapers source is fetched and diffed.
"""
