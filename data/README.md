# Ledger data folder

This folder holds the ledger SQLite database (ledger.db).

The database itself is git-ignored — only this placeholder is tracked
so the folder exists when the repo is cloned fresh.

The ledger is created automatically on first run. Back it up manually
to iCloud, Dropbox, or similar — losing this file means losing the
entire pick history.
