# Optional ML / data stack for the elite NRFI/YRFI engine.
# Install with:  pip install -r nrfi/requirements-nrfi.txt
#
# These are intentionally NOT in pyproject.toml because the core
# Edge Equation engine is deterministic / dependency-light. Add only
# the pieces you need.

# --- Core scientific stack (re-pinning ranges already in pyproject) ---
pandas>=2.2
numpy>=1.26
scipy>=1.11

# --- Modeling ---
xgboost>=2.0
lightgbm>=4.3
scikit-learn>=1.4
shap>=0.45
joblib>=1.3

# --- Data sources ---
pybaseball>=2.2
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.2

# --- Storage & caching ---
duckdb>=1.0
pyarrow>=15.0

# --- Visualization / dashboard ---
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20
streamlit>=1.32

# --- Misc utilities ---
PyYAML>=6.0
tqdm>=4.66
